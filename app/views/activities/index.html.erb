<h1>Recently Logged Hours</h1>


<p><% @activities.each do |activity| %>

<div class="activity">
	 <%= link_to activity.owner.name, activity.owner if activity.owner %>
	<%= render_activity activity %>
</div>
<% end %>
<div class="pull-right">
<%= paginate @activities %></div>
</p>
